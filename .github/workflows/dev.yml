name: ARC Ephemeral Runner Test

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: github-runners

    steps:
      - uses: actions/checkout@v4

      - name: Verify Runner
        run: |
          echo "Hello from ARC"
          hostname
          uname -a
